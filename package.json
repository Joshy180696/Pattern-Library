{
  "name": "styleslibrary",
  "version": "1.0.0",
  "main": "index.js",
"scripts": {
  "build:css": "sass css/styles.scss css/styles.css",
  "build:kss": "kss --config kss-config.json",
  "copy:css": "mkdir -p docs/css && cp css/styles.css docs/css/",
  "build": "npm run build:css && npm run build:kss && npm run copy:css",
  "watch:css": "sass css/styles.scss css/styles.css --watch",
  "test": "echo \"Error: no test specified\" && exit 1"
},
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "kss": "^3.1.0",
    "sass": "^1.69.5"
  }
}

